[package]
name = "my-rust-cli"
version = "0.5.0"
description = "A CLI tool written in Rust"
edition = "2021"
license = "MIT"
rust-version = "1.70"

[[bin]]
name = "my-rust-cli"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
